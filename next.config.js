module.exports = {
  reactStrictMode: true,
  i18n: {
    locales: ['pt-BR','en-US', 'es-ES'],
    defaultLocale: 'pt-BR',
    localeDetection: true    // set to false to disable load the language configured in the user's browser
  }
}
